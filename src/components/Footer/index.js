import React from 'react';

export const Footer = (props) => {
    return (
        <footer className="footer bg-dark pt-5">
            <div className="container">
                <span class="text-muted">Clicky Game!</span>
            </div>
        </footer>
    );
}